import { createTheme } from "@mui/material"

const theme = createTheme({
    palette: {
      primary: {
        main: '#234b24',
        light: '#234b00',
        hoverColor: '#6c351f'
      },
      secondary: {
        main: '#6C351F',
      },
    },
  });

export {theme}